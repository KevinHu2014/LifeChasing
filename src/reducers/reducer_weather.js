// import {  } from '../actions/type';

const INITAL_STATE = {
};

const weather = (state = INITAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default weather;
